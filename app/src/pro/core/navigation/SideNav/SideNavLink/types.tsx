import React from 'react';

type SideNavLinkProps = {
  className?: string;
  iconAngle?: number;
  shouldBeExpanded?: boolean;
  active?: boolean;
  [rest: string]: any;
};

export { SideNavLinkProps };
